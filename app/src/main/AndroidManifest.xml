<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rodrigo.recepcioncel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".subir_datos_web" />
        <activity android:name=".ver_invitados" />
        <activity android:name=".elegir_evento" />
        <activity android:name=".crear_usuarios" />
        <activity android:name=".usuario_logueado" />
        <activity android:name=".sincronizar_con_web"></activity>
    </application>

</manifest>